# Phase 4.5: Création de l'Application Master Admin Séparée

## 🎯 Objectif
Créer une application d'administration globale (`apps/superadmin`) totalement indépendante pour gérer la plateforme SaaS, les souscriptions (tenants), valider les comptes professionnels, et offrir une vue analytique sans mélanger le code du portail client/chauffeur.

## 📋 Tâches (GSD)

- [x] **Tâche 1 :** Scaffold de l'application `apps/superadmin` (Astro/React ou outil dédié comme Twenty/Refine selon la décision finale). Configuration du port 4323.
  → React/Vite (package `superadmin`), port confirmé dans `apps/superadmin/vite.config.ts:9`.
- [x] **Tâche 2 :** Mise en place de l'authentification "Super Admin" (isolation RLS pour que seul le rôle super_admin y accède).
  → `src/pages/Login.tsx` + garde dans `src/layouts/AdminLayout.tsx`, rôle `super_admin` posé par `20260310000000_baseline.sql`.
- [x] **Tâche 3 :** Dashboard de gestion des "Tenants" (Liste des entreprises, statut de validation Kbis, Kill Switch).
  → `src/pages/TenantsList.tsx`, Kill Switch adossé à `20260922000000_tenant_status_kill_switch.sql`.
- [ ] **Tâche 4 :** Analytics globaux (Nombre de courses, Volume d'Affaires).
  → **NON LIVRÉE** (constat du 2026-09-25). `src/App.tsx` ne déclare qu'une route (`TenantsList`).
  Les trois entrées de la sidebar de `src/layouts/AdminLayout.tsx` (Tenants, Analytics, Utilisateurs)
  sont des `href="#"` morts : la nav annonce des écrans qui n'existent pas.
  La ROADMAP a longtemps classé la phase "Complete" en incluant cette vue analytique — c'était faux.

## 🛑 Crash-Test & Validation
- **Sécurité :** S'assurer que le routage et les tokens API de l'app superadmin ont des privilèges élevés correctement protégés.
- **Dépendances :** Réutilisation propre des packages internes `@repo/ui` et `@repo/db` sans créer de fuites de données vers l'app `backoffice`.
