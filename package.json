{
  "name": "vtc-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "format": "turbo run format"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "turbo": "^2.0.0"
  },
  "dependencies": {
    "ws": "^8.21.0"
  }
}
